# 学生管理 MCP 服务设计方案

## 服务名称

学生成长画像与主动帮扶闭环 MCP

## 核心定位

把学生管理从“人工翻表、人工判断、人工写记录”升级为“数据自动汇聚、异常主动发现、帮扶建议生成、过程记录回填、班级态势展示”的闭环式学生管理辅助。

## 适用对象

- 辅导员
- 班主任
- 学工负责人
- 实训指导教师
- 二级学院学生管理人员

## 主要工具

### 1. 查询学生成长画像

工具名：`get_student_profile`

输入：学号或姓名。

输出：

- 学生基本信息
- 学业趋势
- 考勤情况
- 实训日志与实训异常
- 宿舍反馈
- 资助状态
- 谈心谈话记录
- 系统计算的关注等级
- 下一步建议

### 2. 生成需关注学生名单

工具名：`list_attention_students`

输入：关注等级，可选。

输出：

- 中关注/高关注学生列表
- 关注原因
- 关注分值
- 所属班级

### 3. 写入帮扶跟进记录

工具名：`create_followup_record`

输入：

- 学号
- 记录人
- 跟进摘要
- 下一步动作
- 状态

输出：

- 新增记录编号
- 创建时间
- 当前状态

### 4. 生成班级学生状态看板

工具名：`get_class_dashboard`

输入：班级名称。

输出：

- 班级人数
- 低/中/高关注人数
- 近 30 天缺勤次数
- 近 30 天迟到次数
- 实训日志缺交数
- 实训异常数
- 帮扶记录数
- 高关注学生摘要

## 典型展示场景

### 场景 1：辅导员查询单个学生

输入：“查询 S004 的学生画像。”

智能体通过 MCP 获取数据后输出：

- 学生近况摘要
- 主要关注原因
- 需核实信息
- 谈心谈话提纲
- 一周内帮扶建议
- 记录模板

### 场景 2：生成今日需关注名单

输入：“列出今天需要重点关注的学生。”

智能体通过 MCP 返回中/高关注名单，并按关注分值排序，输出：

- 学生名单
- 关注等级
- 关注原因
- 建议跟进人
- 建议完成时限

### 场景 3：谈心后回填记录

输入：“把 S002 的谈心情况记录一下：学生最近课程压力大，实训日志补交意识不足，三天后复访。”

智能体调用 MCP 写入记录，形成台账。

### 场景 4：班级看板

输入：“生成智能制造2401班学生状态看板。”

智能体通过 MCP 输出班级态势，包括关注等级分布、缺勤迟到、实训异常、跟进完成情况。

## 安全边界

1. MCP 只提供学生管理辅助信息，不输出处分、资助、成绩、事故责任等最终认定。
2. 涉及隐私信息时，展示端应脱敏或按角色权限控制。
3. 高关注不等同于心理诊断或纪律结论，只表示需要进一步线下核实。
4. 帮扶记录写入应保留记录人、时间和来源，方便追溯。
5. 比赛演示阶段使用模拟数据，不接入真实敏感数据。

## 材料表达亮点

本 MCP 服务体现了“智能体 + 工具调用 + 数据闭环”的平台能力。它不仅能回答学生管理问题，还能主动查询数据、识别关注对象、生成帮扶建议、回填过程记录，并形成班级态势看板，展示了学生管理数字化从“经验驱动”向“数据驱动、闭环治理”转变。

## 平台接入情况

已查看平台“资源管理 - MCP服务”页面，平台支持：

- 手动创建 MCP 服务
- 从 JSON 导入 MCP 服务
- 链接方式：`sse`
- 配置项：服务名称、描述、接口地址、Header 配置
- 提供“点击测试链接”能力

因此，该 MCP 原型正式接入平台前，需要先部署为平台可访问的 SSE MCP 服务地址。当前本地原型已完成数据、工具逻辑和调用示例，适合作为后端逻辑基础；后续可增加标准 MCP/SSE 适配层后接入平台。

示例导入配置见：

```json
{
  "mcpServers": {
    "student-growth-support": {
      "type": "sse",
      "url": "https://your-domain.example.com/student-management-mcp/sse",
      "headers": {
        "Authorization": "Bearer your-token"
      }
    }
  }
}
```
