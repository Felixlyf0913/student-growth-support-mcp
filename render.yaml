services:
  - type: web
    name: student-growth-support-mcp
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn student_management_mcp_sse:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
