# 学生帮扶任务闭环工具说明

## 功能定位

该能力把学生画像查询、风险研判、任务派发、钉钉触达、进展更新和帮扶记录回写串成完整闭环。

## MCP 工具

### `create_student_support_task`

- 根据学号或姓名读取学生画像。
- 自动带入关注等级、关注分数和风险依据。
- 自动生成默认帮扶目标和建议措施，也可由用户指定。
- 保存负责人、完成期限、优先级和任务编号。
- 默认只创建任务；只有 `push_to_dingtalk=true` 时才推送辅导员或教师工作群。

### `update_student_support_task`

- 支持状态：待处理、跟进中、已完成、已关闭。
- 可更新负责人、期限、本次进展和下一步措施。
- `sync_followup=true` 时同步写入谈心谈话或帮扶记录。
- 只有 `push_update=true` 时才向教师工作群推送进展。

### `list_student_support_tasks`

- 可按学生、负责人和状态查询。
- 支持只查看逾期任务。
- 返回待处理、跟进中、已完成和已关闭数量汇总。

### `get_support_task_audit`

- 查询任务创建和更新轨迹。
- 分别记录操作人、负责人、变更前状态、变更后状态和发生时间。
- 支持按任务编号筛选，便于复盘和责任留痕。

### `get_system_readiness`

- 只读检查 PostgreSQL/SQLite 存储状态、任务数量、审计数量和钉钉渠道是否完成配置。
- 只返回布尔状态和数量，不返回数据库连接串、Webhook 或任何密钥。

## 数据存储

- 生产和比赛演示环境优先使用 `DATABASE_URL` 指向 PostgreSQL。
- 本地未配置数据库时使用 SQLite，方便开发和自动测试。
- 原 JSON 数据只作为首次迁移种子，不再作为运行期任务台账。
- 任务创建和更新均写入独立审计表，分别记录 `created_by`、`updated_by` 和负责人。

## 安全边界

- 创建、更新、写入帮扶记录和推送消息都属于有状态操作，智能体必须在用户明确要求后调用。
- 群消息只发送到已配置的教师或辅导员工作群，不默认提醒全体成员。
- 画像和风险依据仅用于辅助研判，不替代学校正式认定和专业评估。
- Render 环境必须配置 PostgreSQL 才能保证重新部署后任务和审计数据不丢失。

## 演示指令

创建并派发任务：

`请为学生S004创建高关注帮扶任务，由辅导员负责，2026年7月18日前完成谈心，并推送到辅导员工作群，不要@全体。`

查询任务：

`请查询当前由辅导员负责的待处理和逾期帮扶任务。`

完成并回写：

`将任务编号ST...更新为已完成，本次已完成线下谈心，下一步联系任课教师并于一周后复访，同时写入帮扶记录，不推送群消息。`
