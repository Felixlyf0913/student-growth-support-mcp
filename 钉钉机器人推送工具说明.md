# 钉钉机器人推送工具说明

## 目标

为“校务智枢”增加三方应用联动能力：智能体生成班级通知、帮扶提醒或待办事项后，可通过 MCP 工具推送到钉钉群。

## 新增工具

| 工具 | 作用 |
|---|---|
| `send_dingtalk_notice` | 推送班级通知、帮扶提醒、待办事项到钉钉群机器人 |
| `generate_and_send_class_weekly_report` | 汇总班级基础态势和学生工作动态，隐去个人敏感明细后生成周报并推送 |

工具参数 `mention_all` 用于控制是否提醒全体成员：

- 普通通知使用 `false`，也是默认值。
- 用户明确要求“@所有人”“@全体成员”时使用 `true`。

## Render 环境变量

不要把 Webhook 写入代码或提交到 GitHub。需要在 Render 服务中配置环境变量：

```text
DINGTALK_ROBOT_WEBHOOK=你的钉钉机器人 Webhook
DINGTALK_KEYWORD=MCP
```

配置多个群时可使用：

```text
DINGTALK_S604124_WEBHOOK=S604124班级群机器人 Webhook
DINGTALK_TEACHER_WEBHOOK=辅导员或教师工作群机器人 Webhook
```

工具会根据 `target` 自动分流：包含“辅导员、教师、老师”的接收对象走教师工作群；包含“S604124、班级群、学生群”的接收对象走 S604124 班级群。旧的 `DINGTALK_ROBOT_WEBHOOK` 仍可作为兼容兜底。

## 自动分层路由

`send_dingtalk_notice` 支持以下参数：

- `recipient_role=student`：学生通知，按 `target` 中的班级编号选择对应班级群。
- `recipient_role=teacher`：教师或辅导员通知，进入教师工作群。
- `at_mobiles`：完整手机号列表，在目标群中定向 @具体学生或教师。
- `mention_all=true`：仅用于明确要求全员知晓的重要通知。

新增班级群时，推荐使用环境变量命名：

```text
DINGTALK_CLASS_S604124_WEBHOOK=对应班级群 Webhook
DINGTALK_CLASS_P603124_WEBHOOK=对应班级群 Webhook
```

为兼容现有配置，`DINGTALK_S604124_WEBHOOK` 仍可正常使用。

群机器人只支持在所在群内按手机号 @成员，不能直接向个人发起私聊。真正的个人私信需要另行接入钉钉内部应用和通讯录权限。脱敏手机号不能用于 @。

钉钉安全设置中已选择“自定义关键词”时，关键词建议使用：

```text
MCP
```

工具发送的消息会自动带上该关键词。

## 推荐测试问题

```text
请生成一条面向 S604124移动 班的实训日志补交通知，并通过钉钉推送到班级群。
```

```text
请生成一条帮扶复访提醒：学生 S004 需要三天后复访，提醒辅导员联系任课教师了解课程困难，并推送到钉钉。
```

```text
请根据校园反诈知识库，生成一条面向 P603124数媒 班的反诈提醒，并推送到钉钉群。
```

```text
请向 S604124移动班级群推送一条防诈骗安全提醒，并@全体成员。
```

```text
请生成 S604124移动 班本周学生工作周报，隐去学生姓名，推送到辅导员工作群，不需要@全体成员。
```

周报会自动汇总在册人数、性别/专业/年级分布，并在已有数据时统计关注等级、考勤、实训和帮扶记录。尚未接入的动态数据会明确标注，不会编造结果。

## 展示口径

该能力体现“智能体融入真实工作流”：智能体不只生成文字，还可以把班级通知、帮扶提醒和待办事项推送到老师实际使用的协同工具中，形成从智能研判到工作触达的闭环。

## 安全提醒

Webhook 相当于群机器人发送权限，不应公开展示完整地址。演示结束后如有需要，可在钉钉群机器人设置中重置 Webhook。
