# 钉钉机器人推送工具说明

## 目标

为“校策通枢”增加三方应用联动能力：智能体生成班级通知、帮扶提醒或待办事项后，可通过 MCP 工具推送到钉钉群。

## 新增工具

| 工具 | 作用 |
|---|---|
| `send_dingtalk_notice` | 推送班级通知、帮扶提醒、待办事项到钉钉群机器人 |

## Render 环境变量

不要把 Webhook 写入代码或提交到 GitHub。需要在 Render 服务中配置环境变量：

```text
DINGTALK_ROBOT_WEBHOOK=你的钉钉机器人 Webhook
DINGTALK_KEYWORD=校策通枢
```

钉钉安全设置中已选择“自定义关键词”时，关键词建议使用：

```text
校策通枢
```

工具发送的消息会自动带上该关键词。

## 推荐测试问题

```text
请生成一条面向 S604124移动 班的实训日志补交通知，并通过钉钉推送到班级群。
```

```text
请生成一条帮扶复访提醒：学生 S004 需要三天后复访，提醒辅导员联系任课教师了解课程困难，并推送到钉钉。
```

```text
请根据校园反诈知识库，生成一条面向 P603124数媒 班的反诈提醒，并推送到钉钉群。
```

## 展示口径

该能力体现“智能体融入真实工作流”：智能体不只生成文字，还可以把班级通知、帮扶提醒和待办事项推送到老师实际使用的协同工具中，形成从智能研判到工作触达的闭环。

## 安全提醒

Webhook 相当于群机器人发送权限，不应公开展示完整地址。演示结束后如有需要，可在钉钉群机器人设置中重置 Webhook。
